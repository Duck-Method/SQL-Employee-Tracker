const dbConfig = {
    user: 'postgres',
    password: '1234',  // Not secure for production, but okay for local testing
    host: 'localhost',
    port: 5432,
    database: 'SQL_Emp_Tracker'
  };
  
  export default dbConfig;