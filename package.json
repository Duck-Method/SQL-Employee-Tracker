{
  "name": "sql-employee-tracker",
  "version": "1.0.0",
  "description": "This project uses Inquirer and PostgreSQL to create a CMS using the command line",
  "main": "main.js",
  "scripts": {
    "start": "node main.js",
    "init-db": "psql -U postgres -d SQL_Emp_Tracker -f db/schema.sql",
    "seed-db": "psql -U postgres -d SQL_Emp_Tracker -f db/seeds.sql",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^8.2.4",
    "pg": "^8.13.1"
  },
  "type": "module"
}
